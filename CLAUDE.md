# cairn — agent instructions

The rules live in `AGENTS.md`, the canonical agent-instruction file. This stub
exists because Claude Code reads `CLAUDE.md`, not `AGENTS.md`; the import below
expands it at session start. Edit `AGENTS.md`, never this file.

@AGENTS.md
